import { Injectable } from '@nestjs/common';

@Injectable()
export class JogadorSagas {
    // @Span()
    // @Saga()
    // evento(events$: Observable<any>): Observable<void> {
    //     return events$
    //         .pipe(
    //             ofType(ALGUM_EVENTO),
    //             map(event => {
    //                 this._logger.log("inside saga", { event: JSON.stringify(event) });
    //
    //             })
    //         )
    // }
}
